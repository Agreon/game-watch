{
    "version": "0.0.1",
    "name": "@game-watch/queue",
    "private": true,
    "main": "dist/main.js",
    "types": "dist/main.d.ts",
    "scripts": {
        "prebuild": "rimraf dist && rimraf tsconfig.tsbuildinfo",
        "build": "tsc -b ."
    },
    "devDependencies": {
        "eslint": "7.32.0",
        "typescript": "^4.4.4"
    },
    "dependencies": {
        "bullmq": "^1.52.1",
        "dotenv": "^10.0.0"
    }
}
