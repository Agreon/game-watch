import { UseToastOptions } from '@chakra-ui/react';

export const DEFAULT_TOAST_OPTIONS: UseToastOptions = {
    position: 'top-right',
    isClosable: true,
    containerStyle: {
        marginTop: '5rem',
    },
};
